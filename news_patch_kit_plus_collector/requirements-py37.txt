# Python 3.7.x compatible pins (works on 32-bit/64-bit). 
# These are the last widely-available versions supporting Python 3.7 and MySQL 5.7 (via PyMySQL).
# NOTE: For 32-bit Windows, installing scipy/numpy wheels is easier via conda/miniforge. 
# If pip fails to find wheels, use conda to create the env and then `pip install -r` the rest.

numpy==1.21.6
scipy==1.7.3
pandas==1.3.5
scikit-learn==1.0.2
joblib==1.1.0
threadpoolctl==2.2.0

PyYAML==5.4.1
PyMySQL==1.0.2

# (transitive deps pinned conservatively for Py3.7)
python-dateutil==2.8.2
pytz==2021.3
six==1.16.0
